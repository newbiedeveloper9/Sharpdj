<UserControl x:Class="SharpDj.View.UserControls.SdjRegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SharpDj.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="40">
            <Label Content="Login"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Username"/>
            <TextBox Text="{Binding Nickname, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Password"/>
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="E-mail"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Register" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0 10 0 0" Command="{Binding RegisterMeCommand}"/>
        </StackPanel>
        <Label Foreground="Red" Content="{Binding ErrorNotify}" VerticalAlignment="Bottom" FontSize="16" Margin="10"/>
    </Grid>
</UserControl>
